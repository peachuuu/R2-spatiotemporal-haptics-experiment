# 数据导出与存档边界

实验数据由浏览器下载到操作者选择的下载目录，不应写回源代码仓库。正式数据至少包含会话 JSON、原始 CSV、分析表、质量控制表和可能的 ZIP 汇总包。

提交或分享源代码前：

1. 检查仓库中不存在被试编号、姓名、联系方式或自由文本回答。
2. 不提交实验导出的 JSON、CSV、XLSX、ZIP。
3. 不提交 `logs/`、`derived/`、浏览器下载目录或串口诊断日志。
4. 如需演示数据，只能使用明确标注的合成数据，并确保无法对应真实参与者。
5. 正式原始数据应存放在经批准的受控位置，并与源代码存档分离。

